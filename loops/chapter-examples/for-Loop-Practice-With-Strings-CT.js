// Create a string variable containing your name.
let name = "Charlene"

// Write a for loop that prints each character in your name on a different line.

for (let i = 0; i < name.length; i++) {
    console.log(name[i]);                                       // Loop body will print out "name[i]" each time, meaning one of the characters of name
}                                                               // Results printed out a character from name variable on each line. 